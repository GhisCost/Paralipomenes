<footer>
	<nav class="nav-footer">
		<ul class="fond-rouge">
			<a href="" class="eagle">
				<li>Mentions Légales</li>
			</a>
			<a href="" class="eagle">
				<li>Contacts</li>
			</a>
			<a href="" class="eagle">
				<li>F.A.Q</li>
			</a>
		</ul>
	</nav>
</footer>
<script src="{{asset('assets/js/structure.js')}}"></script>
<script src="https://cdn.jsdelivr.net/npm/quill@2.0.3/dist/quill.js"></script>
<script>

{% if hasQuill is defined and hasQuill %}

document.addEventListener('DOMContentLoaded', function () { 
	
// 1) Initialiser Quill
const quill = new Quill('#editor', {theme: 'snow'});

// 2) Récupérer le textarea généré par Symfony
const textarea = document.querySelector('#{{form.contenu.vars.id}}');

// 3) (optionnel) S'assurer que Quill contient le texte initial du textarea
quill.root.innerHTML = textarea.value;

const form = document.querySelector('form[name="{{form.vars.name}}"]');

form.addEventListener('submit', function () {
textarea.value = quill.root.innerHTML;
});
});

{% endif %}

</script>
